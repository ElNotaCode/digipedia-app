import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.digimonpedia',
  appName: 'Digipedia',
  webDir: 'dist/digipedia-app',
  bundledWebRuntime: false,
};

export default config;
